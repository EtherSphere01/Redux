import Home from "../components/Home";

// this is for connecting to redux store
export default function HomeContainer() {
    return (
        <div>
            <div>
                <Home />
            </div>
        </div>
    );
}
